import React from 'react';
import ReactDOM from 'react-dom/client';
import CreateProduct from './App'; 
import './index.css'; 

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CreateProduct />
  </React.StrictMode>
);